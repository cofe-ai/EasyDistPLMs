{
    "train_micro_batch_size_per_gpu": 128,
    "gradient_accumulation_steps": 1,
    "optimizer": {
        "type": "Adam",
        "params": {
        "lr": 1e-5
        }
    },
    "gradient_clipping": 1.0,
    "fp16": {
        "enabled": false
    },
    "amp": {
        "enabled": true,
        "opt_level": "O1",
        "loss_scale": "dynamic"
    }
}